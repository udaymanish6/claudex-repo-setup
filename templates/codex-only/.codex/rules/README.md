# Codex Rules

Put project-local Codex `.rules` files here only when a project needs command approval policy checked into the repo.

Codex loads project-local rules from `.codex/rules/` only in trusted projects.

